add_subdirectory(capicpp)
add_subdirectory(clegacy)
add_subdirectory(cppapi)
add_subdirectory(derived)
if(NOT APPLE AND NOT WIN32)
    add_subdirectory(memory_resource)
endif()
add_subdirectory(ssize)
